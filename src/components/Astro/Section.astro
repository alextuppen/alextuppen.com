---
const { className, ...rest } = Astro.props;
---

<section
  class:list={[
    "w-full max-w-6xl mx-auto px-4 py-10 flex flex-col gap-8 items-center",
    className,
  ]}
  {...rest}
>
  <slot />
</section>
