---
export interface ListItem {
  href: string;
  title: string;
  src: string | undefined;
  alt: string | undefined;
  description: string;
  keywords?: string[] | undefined;
}

interface Props {
  list: ListItem[];
}

const { list } = Astro.props;
const searchEnabled = list.some((item) => item.keywords != null);
---

<div class="w-full flex flex-col gap-6">
  {
    searchEnabled && (
      <div class="w-full max-w-md mx-auto">
        <input
          id="blog-recipe-search"
          type="text"
          placeholder="Search..."
          autocomplete="off"
          class="w-full px-4 py-2.5 bg-surface-1 border border-white/10 rounded-lg text-white placeholder-white/30 focus:outline-none focus:border-primary/60 transition-colors"
        />
      </div>
    )
  }
  <script
    is:inline
    type="application/json"
    id="blog-recipe-list-data"
    set:text={JSON.stringify(list)}
  />
  <ol id="blog-recipe-list" class="w-full flex flex-col gap-4">
    {
      list.map((item, index) => (
        <li
          class="group"
          data-index={index}
        >
          <a
            href={item.href}
            class="flex flex-col md:flex-row bg-surface-1 rounded-xl overflow-hidden hover:bg-surface-2 transition-colors duration-200 shadow-lg shadow-black/20"
          >
            {item.src != null && item.alt != null ? (
              <img
                class="w-full md:w-48 h-48 object-cover shrink-0"
                width={300}
                height={300}
                src={item.src}
                alt={item.alt}
              />
            ) : (
              <div class="w-full md:w-48 h-48 shrink-0 flex items-center justify-center bg-surface-2">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 16 16"
                  fill="currentColor"
                  class="w-16 h-16 text-white/20"
                >
                  <path d="M6.002 5.5a1.5 1.5 0 1 1-3 0 1.5 1.5 0 0 1 3 0" />
                  <path d="M1.5 2A1.5 1.5 0 0 0 0 3.5v9A1.5 1.5 0 0 0 1.5 14h13a1.5 1.5 0 0 0 1.5-1.5v-9A1.5 1.5 0 0 0 14.5 2zm13 1a.5.5 0 0 1 .5.5v6l-3.775-1.947a.5.5 0 0 0-.577.093l-3.71 3.71-2.66-1.772a.5.5 0 0 0-.63.062L1.002 12v.54L1 12.5v-9a.5.5 0 0 1 .5-.5z" />
                </svg>
              </div>
            )}
            <div class="p-5 flex flex-col gap-2 text-left">
              <h2 class="text-lg font-semibold group-hover:underline group-hover:decoration-primary">
                {item.title}
              </h2>
              <p class="text-white/60 text-sm leading-relaxed">{item.description}</p>
            </div>
          </a>
        </li>
      ))
    }
  </ol>
  <p id="blog-recipe-no-results" class="text-white/50 text-center py-8 hidden">
    No results found
  </p>
</div>

<script>
  import Fuse from "fuse.js";

  const dataEl = document.getElementById("blog-recipe-list-data");
  const searchEl = document.getElementById("blog-recipe-search") as HTMLInputElement | null;
  const listEl = document.getElementById("blog-recipe-list");
  const noResultsEl = document.getElementById("blog-recipe-no-results");

  if (dataEl && searchEl && listEl) {
    const list = JSON.parse(dataEl.textContent ?? "[]");
    const fuse = new Fuse(list, {
      keys: ["title", "keywords"],
      includeScore: true,
      threshold: 0.3,
    });

    const allItems = Array.from(listEl.querySelectorAll<HTMLLIElement>("li[data-index]"));

    searchEl.addEventListener("input", () => {
      const value = searchEl.value.trim();

      if (!value) {
        allItems.forEach((item) => {
          item.style.display = "";
          item.style.order = "";
        });
        if (noResultsEl) noResultsEl.classList.add("hidden");
        return;
      }

      const results = fuse.search(value);
      const resultIndices = new Map(results.map((r, i) => [r.refIndex, i]));

      let visibleCount = 0;
      allItems.forEach((item) => {
        const idx = parseInt(item.dataset["index"] ?? "-1");
        if (resultIndices.has(idx)) {
          item.style.display = "";
          item.style.order = String(resultIndices.get(idx));
          visibleCount++;
        } else {
          item.style.display = "none";
        }
      });

      if (noResultsEl) {
        noResultsEl.classList.toggle("hidden", visibleCount > 0);
      }
    });
  }
</script>
