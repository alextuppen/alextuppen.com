---
import { BaseHead, Header, Footer } from "../components";
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
  image?: string | undefined;
}

const { title, description, image }: Props = Astro.props;
---

<html lang="en" class="h-full">
  <head>
    <BaseHead title={title} description={description} image={image} />
  </head>
  <body
    class="bg-base text-white font-sans min-h-full grid grid-rows-[auto_1fr_auto]"
  >
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
